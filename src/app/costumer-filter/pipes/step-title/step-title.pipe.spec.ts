import { StepTitlePipe } from './step-title.pipe';

describe('StepTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new StepTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
