import { RefineButtonPipe } from './refine-button.pipe';

describe('RefineButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new RefineButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
